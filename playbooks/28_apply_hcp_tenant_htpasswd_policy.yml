---
# Backward-compatible entry point. HCP OAuth is now configured directly on the
# HostedCluster resources while RHACM continuously enforces guest cluster RBAC.
- import_playbook: 29_configure_hcp_lab_admin.yml
